## 拖拽

```
	步骤：
		1.  首先要保证元素可以被拖拽，通过设置 draggable = "true"
		
		2.  被拖拽元素会触发一个ondragstart事件，在该事件中通过事件对象参数e,可以对被拖拽数据信息进行操作
				e.dataTransfer.setData(k,value)  设置被拖拽对象的数据
				
		3.  容器在接收被拖拽元素的时候会触发一个ondrop的事件，通过该事件中的事件对象参数可以获取到被拖拽元素的信息
```

